import {authenticated,db,bucket,checkOrigin,HttpError,failure} from '../../server';
export const dynamic='force-dynamic';
export async function GET(req:Request){try{const owner=await authenticated();const id=new URL(req.url).searchParams.get('id');const m=await db().prepare('SELECT type FROM images WHERE id=? AND owner=?').bind(id,owner).first();if(!m)throw new HttpError(404,'Image not found.');const o=await bucket().get(id!);if(!o)throw new HttpError(404,'Image not found.');return new Response(o.body,{headers:{'Content-Type':String(m.type),'Cache-Control':'private, no-store','X-Content-Type-Options':'nosniff'}})}catch(e){return failure(e)}}
export async function POST(req:Request){try{checkOrigin(req);const owner=await authenticated();if(Number(req.headers.get('content-length')||0)>5500000)throw new HttpError(413,'Maximum image size is 5 MB.');const f=(await req.formData()).get('file');if(!(f instanceof File)||f.size>5000000||f.size<12)throw new HttpError(400,'Choose a PNG, JPEG or WebP image under 5 MB.');const bytes=new Uint8Array(await f.arrayBuffer());const png=[137,80,78,71,13,10,26,10].every((v,i)=>bytes[i]===v);const jpeg=bytes[0]===255&&bytes[1]===216&&bytes[2]===255;const webp=new TextDecoder().decode(bytes.slice(0,4))==='RIFF'&&new TextDecoder().decode(bytes.slice(8,12))==='WEBP';const type=png?'image/png':jpeg?'image/jpeg':webp?'image/webp':null;if(!type)throw new HttpError(400,'Only PNG, JPEG and WebP images are supported.');const id=crypto.randomUUID();await bucket().put(id,bytes,{httpMetadata:{contentType:type}});try{await db().prepare('INSERT INTO images(id,owner,type) VALUES(?,?,?)').bind(id,owner,type).run()}catch(e){await bucket().delete(id);throw e}return Response.json({url:'/api/image?id='+id},{headers:{'Cache-Control':'no-store'}})}catch(e){return failure(e)}}
