/**
 * Drizzle schema.
 *
 * Step 2 ports `docs/schema.sql` here in full. It is intentionally empty for
 * now: the scaffold proves the connection works before any table exists, so a
 * connection failure can't be mistaken for a schema problem.
 */
export {};
